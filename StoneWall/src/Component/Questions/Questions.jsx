import React from 'react';
import styles from './Questions.module.css'

const Questions = () => {
  return (
    <div className={`${styles.main} container margin-top`}>
      <h1 className={styles.mainText}>Como funciona?</h1>
    </div>
  );
};

export default Questions;
